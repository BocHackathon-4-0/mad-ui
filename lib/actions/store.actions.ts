'use server';

// export async function fetchStore(storeId: string) {
//     const snapshot =
//         await firebaseDb.collection('stores').get();
//     snapshot.forEach((doc) => {
//         console.log(doc.id, '=>', doc.data());
//     });
// }

